# DIC Analyzer

Полнофункциональная система для анализа цифровых изображений с использованием DIC (Digital Image Correlation).

## 🚀 Быстрый запуск

### Способ 1: Автоматический запуск (Рекомендуется)
```bash
# Просто дважды кликните на файле:
start_servers.bat
```

### Способ 2: Использовать Python скрипт для HTTP сервера
```bash
# Запуск только HTTP сервера для frontend
python run_system.py
```

### Способ 3: Ручной запуск

#### Терминал 1 - Django Backend:
```bash
cd app
python manage.py runserver 8000
```

#### Терминал 2 - Frontend сервер:
```bash
python run_system.py
```

### Способ 4: Остановка серверов
```bash
# Дважды кликните на файле:
stop_servers.bat
```

## 📱 Доступ к приложению

После запуска откройте в браузере: **`http://localhost:8080/working_app.html`**

### Дополнительные ссылки:
- **Django API:** `http://localhost:8000/api/`
- **Django Admin:** `http://localhost:8000/admin/`
- **API документация:** Доступна через браузер на API endpoints

## 📋 Функциональность

### ✅ Реализовано:
- **Создание DIC анализов** с загрузкой изображений
- **Просмотр списка анализов** с статусами и результатами
- **Скачивание результатов** в ZIP архиве (изображения + JSON + статистика) 
- **Система авторизации** (регистрация/вход/выход)
- **CSRF защита** для безопасных запросов
- **RESTful API** для всех операций

### 🎯 Основные возможности:
- Загрузка изображений before/after
- Настройка параметров анализа (subset size, step, iterations, correlation)
- Валидация входных данных
- Асинхронная обработка анализов
- Просмотр результатов и статистики

## 🏗️ Архитектура

```
├── working_app.html      # Основное веб-приложение
├── app/                  # Django backend
│   ├── dic_api/         # REST API приложение
│   ├── dic_algoritm/    # Алгоритмы DIC анализа
│   └── db.sqlite3       # База данных (SQLite по умолчанию)
├── media/               # Загруженные файлы и результаты
├── run_system.py        # Скрипт запуска HTTP сервера
├── start_servers.bat    # Автозапуск всех серверов (Windows)
├── stop_servers.bat     # Остановка всех серверов (Windows)
└── venv/                # Виртуальное окружение Python
```

## 🗄️ База данных

### Текущая конфигурация: SQLite
Приложение использует SQLite по умолчанию для простоты развертывания.
``

## 🔧 API Endpoints

### Анализы:
- `GET/POST /api/analyses/` - список/создание анализов
- `GET /api/analyses/{id}/` - детали анализа
- `POST /api/analyses/{id}/cancel/` - отмена выполняющегося анализа
- `GET /api/analyses/{id}/download/` - скачивание результатов в ZIP (изображения, JSON, статистика)

### Аутентификация:
- `POST /api/auth/register/` - регистрация
- `POST /api/auth/login/` - вход
- `POST /api/auth/logout/` - выход
- `GET /api/get-csrf-token/` - получение CSRF токена

## 🎨 Интерфейс

Приложение имеет современный веб-интерфейс с:
- Адаптивным дизайном
- Интуитивным управлением
- Статусами в реальном времени
- Обработкой ошибок

## 📊 Использование

1. **Регистрация/Вход** в систему
2. **Создание анализа:**
   - Выбрать изображение "до" деформации
   - Выбрать изображение "после" деформации
   - Настроить параметры анализа
   - Нажать "Start Analysis"
3. **Просмотр результатов** в списке анализов
4. **Скачивание результатов** - для завершенных анализов доступна кнопка "📥 Download Results"

## 🔒 Безопасность

- CSRF защита для всех форм
- Валидация входных данных
- Безопасная обработка файлов
- Аутентификация пользователей

## 📈 Производительность

- Асинхронная обработка анализов
- Оптимизированные алгоритмы
- Эффективное хранение результатов

---
